global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/caseops.rules.yml

scrape_configs:
  - job_name: caseops-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]
        labels:
          service: caseops-api
